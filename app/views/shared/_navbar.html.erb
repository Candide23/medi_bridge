<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
  <div class="container">
    <%= link_to "MediBridge", root_path, class: "navbar-brand fw-bold text-primary" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
      <ul class="navbar-nav align-items-center">
        <% if user_signed_in? %>
          <li class="nav-item me-2">
            <span class="nav-link text-dark fw-semibold">
              👋 <%= current_user.first_name %>
            </span>
          </li>
          <li class="nav-item">
            <%= button_to "Logout", destroy_user_session_path, method: :delete, form: { class: "d-inline" }, class: "btn btn-outline-primary" %>
          </li>
        <% else %>
          <li class="nav-item me-2">
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>




