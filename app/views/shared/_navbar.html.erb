<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm px-4 py-2">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2 fw-bold text-primary" do %>
      <i class="bi bi-hospital-fill fs-4"></i> MediBridge
    <% end %>

    <div class="d-flex align-items-center gap-3 ms-auto">
      <% if user_signed_in? %>
        <div class="dropdown">
          <button class="btn btn-outline-light d-flex align-items-center gap-2" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="badge bg-info rounded-circle text-white fs-6" style="width: 36px; height: 36px;">
              <%= current_user.first_name.first.upcase %>
            </span>
            <span class="fw-semibold text-dark">
              👋 <%= current_user.first_name %> <%= current_user.last_name %>
            </span>
            <i class="bi bi-caret-down-fill text-muted"></i>
          </button>

          <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
            <li>
              <%= link_to 'Edit Profile', edit_user_registration_path, class: "dropdown-item" %>
            </li>
            <li><hr class="dropdown-divider"></li>
            <!-- Logout -->
            <li>
              <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "m-0" do %>
                <button type="submit" class="dropdown-item d-flex align-items-center gap-2 text-danger border-0 bg-transparent w-100">
                  <i class="bi bi-box-arrow-right"></i>
                  Logout
                </button>
              <% end %>
            </li>
          </ul>
        </div>
      <% else %>
        <!-- Guest Actions -->
        <div class="d-flex gap-2">
          <%= link_to new_user_session_path, class: "btn btn-outline-primary d-flex align-items-center gap-1" do %>
            <i class="bi bi-box-arrow-in-right"></i>
            Login
          <% end %>
          
          <%= link_to new_user_registration_path, class: "btn btn-primary d-flex align-items-center gap-1" do %>
            <i class="bi bi-person-plus"></i>
            Sign Up
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</nav>




