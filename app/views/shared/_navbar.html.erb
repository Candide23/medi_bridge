<nav class="navbar navbar-expand-lg bg-white border-bottom py-3 px-4">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <!-- Brand -->
    <%= link_to root_path, class: "d-flex align-items-center text-decoration-none" do %>
      <div class="me-2">
        <i class="bi bi-hospital-fill text-primary fs-4"></i>
      </div>
      <span class="fw-bold fs-4 text-primary">MediBridge</span>
    <% end %>

    <!-- Right section -->
    <div class="d-flex align-items-center">
      <% if user_signed_in? %>
        <div class="me-3 d-flex align-items-center">
          <span class="me-2 fs-5">👋</span>
          <div class="avatar bg-info text-white rounded-circle d-flex justify-content-center align-items-center me-2" style="width: 32px; height: 32px;">
            <%= current_user.first_name.first.upcase %>
          </div>
          <span class="fw-semibold"><%= current_user.first_name %> <%= current_user.last_name %></span>
        </div>

      <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm d-flex align-items-center" do %>
        <i class="bi bi-box-arrow-right me-1"></i>
        <span>Logout</span>
      <% end %>

      <% else %>
        <div class="d-flex gap-2">
          <%= link_to new_user_session_path, class: "btn btn-outline-primary d-flex align-items-center" do %>
            <i class="bi bi-box-arrow-in-right me-1"></i> Login
          <% end %>

          <%= link_to new_user_registration_path, class: "btn btn-primary d-flex align-items-center" do %>
            <i class="bi bi-person-plus me-1"></i> Sign Up
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</nav>





