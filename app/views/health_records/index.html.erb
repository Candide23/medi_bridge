<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="fw-bold">Your Health Records</h1>
    <%= link_to "Upload New Record", new_health_record_path, class: "btn btn-primary" %>
  </div>

  <% if @health_records.blank? %>
    <div class="alert alert-info d-flex justify-content-between align-items-center px-4 py-3 shadow-sm">
      <span>You have no health records yet.</span>
      <%= link_to "Upload One Now", new_health_record_path, class: "btn btn-sm btn-primary" %>
    </div>
  <% else %>
    <div class="table-responsive shadow-sm">
      <table class="table table-bordered table-hover align-middle">
        <thead class="table-light">
          <tr>
            <th>Document Type</th>
            <th>Language</th>
            <th>File</th>
            <th class="text-center">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @health_records.each do |record| %>
            <tr>
              <td><%= record.record_type.titleize %></td>
              <td><%= record.language.capitalize %></td>
              <td>
                <% if record.document.attached? %>
                  <%= link_to "Download", rails_blob_path(record.document, disposition: "attachment"), class: "btn btn-sm btn-outline-primary" %>
                <% else %>
                  <span class="text-muted">No file</span>
                <% end %>
              </td>
              <td class="text-center">
                <%= link_to "Show", record, class: "btn btn-sm btn-light" %>
                <%= link_to "Edit", edit_health_record_path(record), class: "btn btn-sm btn-outline-secondary" %>
                <%= button_to "Delete", record, method: :delete,
                      data: { confirm: "Delete this record?" },
                      class: "btn btn-sm btn-outline-danger" %>
              </td>

            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>


